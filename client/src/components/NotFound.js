import React from 'react';

const NotFound = () => {
    return (
        <div className="container">
            <h1>Not found <span>:(</span></h1>
            <p>Sorry, but the page you were trying to access does not exist.</p>
        </div>
    );
};

export default NotFound;